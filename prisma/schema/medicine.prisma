model Medicines{
    id String @id @default(uuid())
    name String
    price Int
    stock Int
    manufacturer String
    img_url String
    description String? 

    category Categories @relation(fields:[category_id],references:[id],onDelete: Cascade,onUpdate: Cascade)
    category_id Int

    seller User @relation(fields:[seller_id],references: [id],onDelete: Cascade,onUpdate: Cascade)
    seller_id String 

    orderItems OrderItem[]

    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt

    @@index([id])
    @@index([seller_id])
    @@map("medicines")
}